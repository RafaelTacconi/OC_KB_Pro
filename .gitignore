# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Streamlit
.streamlit/secrets.toml

# Runtime data (SQLite DB + uploaded sources) — created on first run
data/

# Deployment configuration with real credentials — never committed (SPEC §14.2)
.env

# OS
.DS_Store
Thumbs.db

# Test / coverage
.pytest_cache/
.coverage
htmlcov/

# Real-corpus positive-case regression set — off-repo only, never committed
GROUNDING_REGRESSION_POSITIVE.md